// import { customFetch } from '../api';
// import { AdsPagination } from '../models/types';

// const url = () => `search`;

// export const loader = async (): Promise<number> => {
//   const response = await customFetch(url());
//   const adsResponse: AdsPagination = response.data as AdsPagination;

//   console.log('🚀 ~ loaderWithTotalValue ~ total:', adsResponse.total.value);

//   return adsResponse.total.value;
// };
